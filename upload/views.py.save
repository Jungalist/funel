from django.shortcuts import render
from django.http import HttpResponse
from .forms import UploadFileForm

# Create your views here.
#def upload_view(request):
#    return render(request, 'upload/upload_view.html', {})

def upload(request):
    def handle_uploaded_file(f):
         with open('/home/seb/project/djangoservice/media/data_', 'wb+') as destination:
            for chunk in f.chunks():
                destination.write(chunk)
   
    if request.method == 'POST':
        form = UploadFileForm(request.POST, request.FILES)
        if form.is_valid():
            handle_uploaded_file(request.FILES['file'])
            return HttpResponse('/success/url/')
    else:
        form = UploadFileForm()
    return render(request, 'upload/upload_view.html', {'form': form})

#handle_uploaded_file(request.FILES['file'])
   
